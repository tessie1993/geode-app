# Provenance of the prebuilt library committed in this directory.
#
# Built locally in the session worktree, replicating .github/workflows/native-libs.yml:
#   NDK 28.0.13004108, ANDROID_PLATFORM=android-26, ANDROID_ABI=x86_64,
#   -DBUILD_SHARED_LIBS=ON -DENABLE_PLAYLIST=ON, Release, Ninja,
#   -Wl,-z,max-page-size=16384,-z,common-page-size=16384
# projectM v4.1.7, stock (no patches); unstripped, as the workflow produces.
# The JNI bridge (libmilkdropjni.so) is not committed: the app build compiles
# it from app/src/main/cpp with the NDK.
#
# Verified before commit: every ELF LOAD segment aligned 0x4000 (16 KB).
# Commit this file alongside the .so it describes;
# android.yml verifies the hash on every pull request.
1b4137ab92013a1d53c877c4640e5b2ca375e668df0f4f3bad30fc430ea3d3ca  libprojectM-4.so
